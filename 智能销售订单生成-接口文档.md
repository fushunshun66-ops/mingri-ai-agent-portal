# 智能销售订单生成V02 - 接口文档

## 一、文档概述

**基础请求地址**：`https://aiagentforce.marketingforce.com/api`

**智能体编码**：`agent-326bfec623fc40b58d7b4a2`

---

## 二、接口清单

| 序号 | 接口名称 | 请求方式 | 接口地址 | 说明 |
|------|----------|----------|----------|------|
| 1 | Session创建 | GET | `/open/v1/session/{agentSn}` | 创建会话，获取sessionSn |
| 2 | 文档上传 | POST | `/open/v1/file/upload` | 上传智能体运行所需文档 |
| 3 | 对话流 | POST | `/open/v1/chatFlow/run/{agentSn}` | 发送对话请求，生成销售订单 |

---

## 三、调用流程

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   1. 创建Session │ --> │  2. 上传文档    │ --> │  3. 对话流调用   │
│   (获取sessionSn)│     │  (获取fileSn)   │     │  (生成销售订单) │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

**调用顺序说明**：
1. **Session创建**：获取会话编码 `sessionSn`，用于后续对话的上下文保持
2. **文档上传**（可选）：如需上传销售订单模板或参考文档，调用此接口获取 `fileSn`
3. **对话流调用**：传入 `sessionSn` 和 `versionSn`，通过 `user.query` 输入销售订单生成指令，Agent将返回生成的销售订单内容

---

## 四、接口详情

### 4.1 Session创建

#### 接口说明

使用Agent进行连续对话前，需要开启一段会话，可以使多段对话间的内容互相不产生干扰，可以通过此接口开启新的会话。

#### 请求信息

| 项目 | 内容 |
|------|------|
| 请求方式 | GET |
| 请求地址 | `/open/v1/session/{agentSn}` |

#### 请求参数

**Path参数**：

| 参数名 | 参数类型 | 是否必须 | 参数描述 |
|--------|----------|----------|----------|
| agentSn | string | 是 | 智能体编码，示例：`agent-326bfec623fc40b58d7b4a2` |

#### 请求头

| 参数名 | 参数值 | 描述 |
|--------|--------|------|
| X-API-TOKEN | {token} | API认证令牌 |
| Content-Type | application/json | 请求内容类型 |

#### 请求示例

```bash
curl 'https://aiagentforce.marketingforce.com/api/open/v1/session/agent-326bfec623fc40b58d7b4a2' \
--header 'X-API-TOKEN: {token}' \
--header 'Content-Type: application/json'
```

#### 返回参数

| 参数名 | 参数类型 | 参数描述 |
|--------|----------|----------|
| data.sessionSn | string | 会话编码 |

#### 返回示例

```json
{
    "code": 0,
    "success": true,
    "data": {
        "sessionSn": "session-80adcf0f85b640f09fcda"
    },
    "message": "操作成功"
}
```

---

### 4.2 文档上传

#### 接口说明

上传智能体运行所需要的文档，如销售订单模板、历史数据等。

#### 请求信息

| 项目 | 内容 |
|------|------|
| 请求方式 | POST |
| 请求地址 | `/open/v1/file/upload` |

#### 请求参数

**Body参数（form-data）**：

| 参数名 | 参数类型 | 是否必须 | 参数描述 |
|--------|----------|----------|----------|
| file | MultipartFile | 是 | 文件 |

#### 请求头

| 参数名 | 参数值 | 描述 |
|--------|--------|------|
| X-API-TOKEN | {token} | API认证令牌 |
| Content-Type | multipart/form-data | 请求内容类型 |

#### 请求示例

```bash
curl -X POST 'https://aiagentforce.marketingforce.com/api/open/v1/file/upload' \
--header 'X-API-TOKEN: {token}' \
--header 'Content-Type: multipart/form-data' \
-F "file=@test.pdf"
```

#### 返回参数

| 参数名 | 参数类型 | 参数描述 |
|--------|----------|----------|
| data.fileSn | string | 文件唯一标识 |
| data.fileName | string | 文件名称 |
| data.fileType | string | 文件类型 |
| data.fileSize | int | 文件大小 |

#### 返回示例

```json
{
    "code": 0,
    "success": true,
    "data": {
        "fileSn": "fs-1a35013248774bdbba8a5bf",
        "fileName": "demo.xls",
        "fileType": "xls",
        "fileSize": "223"
    },
    "message": "操作成功"
}
```

---

### 4.3 对话流（ChatFlow）

#### 接口说明

向当前Agent发送会话请求并接收回复消息的接口，用于触发智能销售订单的生成。

#### 请求信息

| 项目 | 内容 |
|------|------|
| 请求方式 | POST |
| 请求地址 | `/open/v1/chatFlow/run/{agentSn}` |

#### 请求参数

**Path参数**：

| 参数名 | 参数类型 | 是否必须 | 参数描述 |
|--------|----------|----------|----------|
| agentSn | string | 是 | 智能体编码，示例：`agent-326bfec623fc40b58d7b4a2` |

**Body参数**：

| 参数名 | 参数类型 | 是否必须 | 参数描述 |
|--------|----------|----------|----------|
| sessionSn | string | 是 | 会话编码，调用session创建接口获取 |
| versionSn | string | 是 | 版本编码，右上角复制智能体版本 |
| stream | boolean | 否 | 是否流式输出，默认流式 |
| callbackUrl | string | 否 | 异步回调地址 |
| user | object | 是 | 用户输入 |
| user.query | text | 否 | query |

#### 请求头

| 参数名 | 参数值 | 描述 |
|--------|--------|------|
| X-API-TOKEN | {token} | API认证令牌 |
| Content-Type | application/json; charset=utf-8 | 请求内容类型 |

#### 请求示例

```bash
curl 'https://aiagentforce.marketingforce.com/api/open/v1/chatFlow/run/agent-326bfec623fc40b58d7b4a2' \
  -H 'X-API-TOKEN: {token}' \
  -H 'Content-Type: application/json; charset=utf-8' \
  --data-raw '{
    "sessionSn": "{sessionSn}",
    "versionSn": "version-5aa04294814046f5a70fae3",
    "stream": true,
    "user": {
        "query": null
    }
}'
```

#### 返回参数

| 参数名 | 参数类型 | 参数描述 |
|--------|----------|----------|
| data.sessionSn | string | 会话编码 |
| data.runId | int | 执行ID |
| data.messageTime | string | 消息时间 |
| data.messageStatus | string | 消息状态：success（正常）、fail（异常） |
| data.runStatus | string | 运行状态：running（进行中）、complete（结束） |
| data.content | object | 消息体 |

#### 返回示例

**示例一：信息缺失（缺少关键要素）**

```json
{
    "code": 0,
    "success": true,
    "data": {
        "sessionSn": "session-80adcf0f85b640f09fcda",
        "runId": 12345,
        "messageTime": "2025-07-08T10:30:00",
        "messageStatus": "success",
        "runStatus": "complete",
        "content": {
            "var_5ae26a30802a45528256": {
                "Content": "[信息缺失] 该指令缺少关键要素「单价」。"
            }
        }
    },
    "message": "操作成功"
}
```

**示例二：订单创建成功**

```json
{
    "code": 0,
    "success": true,
    "data": {
        "sessionSn": "session-80adcf0f85b640f09fcda",
        "runId": 12346,
        "messageTime": "2025-07-08T10:35:00",
        "messageStatus": "success",
        "runStatus": "complete",
        "content": {
            "var_5ae26a30802a45528256": {
                "customer_name": "上海华塑物资有限公司",
                "product_details": [
                    {
                        "name": "万华聚氯乙烯七型",
                        "quantity": "500 吨",
                        "unit_price": "8000"
                    }
                ]
            }
        }
    },
    "message": "操作成功"
}
```

**示例三：订单创建成功（含订单编码）**

```json
{
    "code": 0,
    "success": true,
    "data": {
        "sessionSn": "session-80adcf0f85b640f09fcda",
        "runId": 12347,
        "messageTime": "2025-07-08T10:40:00",
        "messageStatus": "success",
        "runStatus": "complete",
        "content": {
            "var_5ae26a30802a45528256": {
                "message": "订单创建成功",
                "orderCode": "SO260710000001"
            }
        }
    },
    "message": "操作成功"
}
```

---

## 五、附录

### 5.1 智能体信息

| 项目 | 值 |
|------|-----|
| 智能体编码 | `agent-326bfec623fc40b58d7b4a2` |
| 基础请求地址 | `https://aiagentforce.marketingforce.com/api` |

### 5.2 状态码说明

| 状态码 | 说明 |
|--------|------|
| 0 | 操作成功 |
| 其他 | 操作失败，详见返回message字段 |

### 5.3 消息状态说明

| 状态 | 说明 |
|------|------|
| success | 消息处理正常 |
| fail | 消息处理异常 |

### 5.4 运行状态说明

| 状态 | 说明 |
|------|------|
| running | 执行进行中 |
| complete | 执行结束 |
